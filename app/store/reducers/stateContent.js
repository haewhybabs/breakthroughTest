import { ADD_TO_CART, REMOVE_FROM_CART,UPDATE_QUANTITY,CLEAR_CART } from '../../constants/const_strings';
export default function stateContent(state = [], action = {}) {
    // switch (action.type) {
       
    // }
    return state
}